// Text for these lives in src/locales/*.json
// (see howItWorks.steps.<key> and faq.items.<key>).

export const HOW_IT_WORKS_KEYS = ['profile', 'groups', 'feed', 'services'];

export const FAQ_KEYS = ['free', 'multi-pet', 'business', 'safety', 'platforms'];
